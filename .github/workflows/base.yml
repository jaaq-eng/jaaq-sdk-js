name: Reusable CI Base

on:
  workflow_call:
    inputs:
      environment:
        required: true
        type: string

jobs:
  ci:
    runs-on: ubuntu-latest
    environment: ${{ inputs.environment }}
    permissions:
      id-token: write
      contents: read
    env:
      NODE_ENV: ${{ inputs.environment }}
      JAAQ_API_URL: ${{ vars.JAAQ_API_URL }}
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 10.17.1

      - uses: actions/setup-node@v4
        with:
          node-version: '22.x'
          cache: 'pnpm'

      - name: Show versions and environment
        run: |
          node -v
          pnpm -v
          echo "ENVIRONMENT=${{ inputs.environment }}"
          echo "NODE_ENV=${{ env.NODE_ENV }}"
          echo "JAAQ_API_URL=${{ env.JAAQ_API_URL }}"

      - name: audit dependencies
        run: pnpm run audit:moderate

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run unit tests
        run: pnpm test

      - name: Run coverage tests
        run: pnpm run test:coverage

      - name: Build
        run: pnpm build

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ vars.AWS_ROLE_ARN }}
          aws-region: ${{ vars.AWS_REGION }}

      - name: Upload CJS to S3
        env:
          S3_BUCKET: ${{ vars.S3_BUCKET }}
        run: |
          VERSION=$(node -p "require('./package.json').version")
          aws s3 cp dist/index.cjs s3://$S3_BUCKET/v$VERSION/jaaq-sdk.min.js

      - name: Smoke tests
        run: pnpm run test:smoke:ci

      - name: Publish info
        run: |
          if [ "${{ inputs.environment }}" = "development" ]; then
            echo "Publish (dev pre-release): set NPM_TOKEN; pnpm version prerelease --preid dev && pnpm publish --tag dev --access public";
          elif [ "${{ inputs.environment }}" = "staging" ]; then
            echo "Publish (rc pre-release): set NPM_TOKEN; pnpm version prerelease --preid rc && pnpm publish --tag rc --access public";
          elif [ "${{ inputs.environment }}" = "production" ]; then
            echo "Publish (stable): set NPM_TOKEN; pnpm version <major|minor|patch> && pnpm publish --tag latest --access public";
          fi
